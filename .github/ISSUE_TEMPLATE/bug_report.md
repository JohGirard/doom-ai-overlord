---
name: Bug report
about: Something doesn't work as documented
title: ""
labels: bug
---

**Scenario** (`defend_the_center.cfg`, `basic.cfg`, `deadly_corridor.cfg`, other):

**Command used**:

```bash
uv run doom-agent ...
```

**What happened**:

**What you expected** (docs/behavior):

**Log excerpt** (the decision lines around the problem):

**Environment**: GPU model, torch/CUDA version (`uv run python -c "import torch; print(torch.__version__, torch.cuda.is_available())"`).
